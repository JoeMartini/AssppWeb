fix: use bag auth endpoint directly + match ipatool Content-Type

Revert auth URL overrides. ipatool uses the bag authenticateAccount
endpoint (buy.itunes.apple.com) directly with a valid SAP signature
and it works. Our overrides to auth.itunes.apple.com caused 204
responses due to different SAP validation per endpoint.

Also change Content-Type from application/x-apple-plist to
application/x-www-form-urlencoded to match ipatool exactly.
